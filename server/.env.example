# No secrets are committed. Use environment variables for real deployments.

# Server
PORT=4000

# Auth (dev-only fallbacks exist if unset)
JWT_SECRET=replace-me
MFA_SECRET_KEY=replace-me-32-chars-min

